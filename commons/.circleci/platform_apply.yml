jobs:
  tf-apply:
    parameters:
      target-env:
        type: string
      appName:
        type: string
    executor:
      name: terraform/default
      tag: 1.2.7
    steps:
      - attach_workspace:
          at: .
      - terraform/apply:
          path: infrastructure/<<parameters.appName>>/<<parameters.target-env>>