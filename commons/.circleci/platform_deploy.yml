jobs:
  plan-apply:
    parameters:
      target-env:
        type: string
      appName:
        type: string
    steps:
      - tf-plan:
          appName: <<parameters.appName>>
          target-env: <<parameters.target-env>>
      - tf-apply:
          appName: <<parameters.appName>>
          target-env: <<parameters.target-env>>
      - persist_to_workspace:
          root: .
          paths:
            - .