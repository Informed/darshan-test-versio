jobs:
  plan-apply:
    executor: terraform
    working_directory: /tmp/project
    parameters:
      target-env:
        type: string
      appName:
        type: string
    steps:
      - tf-plan:
          appName: <<parameters.appName>>
          target-env: <<parameters.target-env>>
      - tf-apply:
          appName: <<parameters.appName>>
          target-env: <<parameters.target-env>>
